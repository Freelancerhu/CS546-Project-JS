<div class="row">
    {{# each products}}
        <div class="row">
        {{# each this}}
        <div class="col-sm-6 col-md-4">
            <div class="thumbnail">
            <img src="{{this.productImage}}" alt="..." class="img-responsive">
            <div class="caption">
                <h3>{{this.title}}</h3>
                <div class="clearfix">
                <div class="price">${{this.price}}</div>
                <a href="/cart/add-to-cart/{{this._id}}" class="btn btn-default pull-right" role="button">Add to Cart</a></p>
                </div>
            </div>
            </div>
            {{/each}}
        </div>
        {{/each}}

</div>
<!-- /.row -->

      